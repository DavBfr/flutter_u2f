name: u2f_example
description: Demonstrates how to use the u2f plugin.
publish_to: "none"

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  convert:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  logging: ^1.0.2
  u2f:
    path: ../
  pointycastle: ^3.4.0

dev_dependencies:
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
